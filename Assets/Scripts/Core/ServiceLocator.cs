using UnityEngine;

/// <summary>
/// For global dependancy injection
/// </summary>
public class ServiceLocator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
